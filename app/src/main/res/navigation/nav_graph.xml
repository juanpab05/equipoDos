<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="com.example.pico_botella.view.FragmentHome"
        android:label="FragmentHome"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_fragmentHome_to_fragmentRules"
            app:destination="@id/fragmentRules" />
    </fragment>

    <fragment
        android:id="@+id/fragmentRules"
        android:name="com.example.pico_botella.view.FragmentRules"
        android:label="FragmentRules"
        tools:layout="@layout/fragment_rules" />

</navigation>